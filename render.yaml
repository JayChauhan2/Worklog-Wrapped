services:
  - type: web
    name: worklog-wrapped
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false   # You'll set this manually in the Render dashboard
      - key: NODE_VERSION
        value: 20
